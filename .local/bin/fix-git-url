#!/bin/bash

git_url="ssh://git@git.server01.lan:222/r"
remote_name="$(git remote get-url origin | sed 's/^.*\///g' | tr -d '\n')"

git remote remove origin
echo "set origin to $git_url/$remote_name"
git remote add origin "$git_url/$remote_name"
