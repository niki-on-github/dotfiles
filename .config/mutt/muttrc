# HowTo:
# 1. Enable IMAP: In gmail, go click the gear icon, go to Settings, go to the tab Forwarding POP/IMAP, and click the Configuration instructions link 
#    in IMAP Access row. Then enable IMAP.
# 2. Enable less secure apps: Before proceeding visit https://www.google.com/settings/security/lesssecureapps and check Turn on radio button.

set ssl_starttls=yes
set ssl_force_tls=yes
set imap_user = "user@gmail.com"
set from = "user@gmail.com"
set realname = "Vorname Nachname"
set folder = imaps://imap.gmail.com/
set spoolfile = imaps://imap.gmail.com/INBOX
set postponed="imaps://imap.gmail.com/[Gmail]/Drafts"
set smtp_url = "smtps://user@smtp.gmail.com"
set move = no
set imap_keepalive = 900
